# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Currency migration backups (operational artifacts, contain user bag data)
functions/scripts/backups/

# Gaia profile export data (member content; regenerate with scrape.py/organize.py,
# already uploaded to Firestore by upload-gaia-exports.mjs). Scripts stay tracked.
scripts/gaia-export/raw/
scripts/gaia-export/accounts.json
scripts/gaia-export/accounts-xlsx.json
scripts/gaia-export/gap-report.md

# Local machine-specific tool settings (not shared)
.claude/settings.local.json

# One-off debug/lookup scratch scripts (may embed a member email; never commit)
*.tmp.mjs

# Firebase emulator logs
firestore-debug.log
firebase-debug.log
