/**
 * The guild's public contact address. Kept in one place because it appears in
 * the policy documents (where a reachable contact is a legal expectation for
 * privacy and data requests) and on the donation wing.
 */
export const SUPPORT_EMAIL = "support@snagemguild.com";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
